<template>
  <view>{{gf}}</view>
  <repeat for="{{renderList}}" key="blockIndex" index="blockIndex" item="blockRender">
    <fa :id.sync="blockRender.url"></fa>
  </repeat>
</template>

<script>
import wepy from 'wepy'
import fa from '../components/father'

export default class GrandFather extends wepy.page {
  config = {}
  components = {
    fa
  }
  data = {
    gf: 3,
    renderList: [
      {url:'www.xxx.com/1'},
      {url:'www.xxx.com/2'}
    ]
  }
}
</script>
